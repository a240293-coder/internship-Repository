{
  "name": "internhood-backend-stub",
  "version": "0.0.0",
  "private": true,
  "description": "Stub package.json — backend removed. Use external backend and set frontend env vars.",
  "scripts": {
    "start": "node server.js",
    "postinstall": "echo 'Backend code removed — this is a stub.'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
