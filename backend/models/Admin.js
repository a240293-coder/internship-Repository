const { DataTypes } = require('sequelize');
const sequelize = require('../config/database');
const bcrypt = require('bcryptjs');

// Admin model stub — local backend removed.
module.exports = {};

module.exports = Admin;
